
  # Ocean Export Workbench UI

  This is a code bundle for Ocean Export Workbench UI. The original project is available at https://www.figma.com/design/lmtXeRXj6pKjmWvpDo8P0x/Ocean-Export-Workbench-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  